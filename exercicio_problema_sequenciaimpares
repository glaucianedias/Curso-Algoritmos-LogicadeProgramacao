Algoritmo "problema_sequenciaimpares"

Var

   x, i : inteiro

Inicio

   escreva("Digite o valor de X: ")
   leia(X)
   
   para i de 1 ate x faca
      se i mod 2 <> 0 entao
         escreval(i)
      fimse
   fimpara
   
Fimalgoritmo
